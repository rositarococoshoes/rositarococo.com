---
export interface Props {
  class?: string;
}

const { class: className = '', ...props } = Astro.props;
---

<div class="rounded-lg border bg-card text-card-foreground shadow-sm {className}" {...props}>
  <slot />
</div>
